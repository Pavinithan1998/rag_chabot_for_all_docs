def doc_qa_tools_demo():
    return f"""            
    <h4>📂 {("Document Ingestion into Vector Store:")}</h4>
    <p>{("Upload and store documents (PDF, Docx, Ppt) to a vector database for fast, efficient semantic search and retrieval.")}</p>

    <h4>💬 {("Chat with the Vector Store:")}</h4>
    <p>{("Interact with stored documents via a chatbot that provides personalized, context-aware responses.")}</p>

    <h4>❓ {("Question Answering with Source Retrieval:")}</h4>
    <p>{("Answer questions by retrieving relevant information from documents, ensuring accuracy and traceability.")}</p>

    <h4>🔑 {("Extract Key Information:")}</h4>
    <p>{("Automatically extract important details like names, dates, or key terms using NLP.")}</p>

    <h4>📝 {("Summarize Documents:")}</h4>
    <p>{("Create clear, concise summaries of complex or long documents for easier understanding.")}</p>

    """
